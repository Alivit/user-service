spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  data:
    redis:
      host: redis
logging:
  level:
    com.minispring.userservice: INFO
    org.hibernate.SQL: ERROR