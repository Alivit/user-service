spring:
  datasource:
    url: ${DOCKER_DB_URL}
    username: ${DOCKER_DB_USER}
    password: ${DOCKER_DB_PASSWORD}
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        generate_statistics: ${HIBERNATE_STATISTICS_ENABLED:false}
  data:
    redis:
      host: ${REDIS_HOST_DOCKER:redis}
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://keycloak:8080/realms/dev/protocol/openid-connect/certs
  grpc:
    client:
      channels:
        auth-service:
          address: static://auth-service:9090
decorator:
  datasource:
    datasource-proxy:
      query:
        enable-logging: ${DB_QUERY_LOGGING_ENABLED:false}
      slow-query:
        enable-logging: ${DB_SLOW_QUERY_LOGGING_ENABLED:false}
        log-level: warn
logging:
  level:
    com.minispring.userservice: ${LOG_LEVEL_APP:INFO}
    org.hibernate.SQL: ${LOG_LEVEL_HIBERNATE:OFF}
    org.zalando.logbook: ${LOG_LEVEL_LOGBOOK:OFF}
app:
  cache:
    lifecycle:
      default: ${CACHE_DEFAULT:1h}
      user-info: ${CACHE_USER_INFO:24h}
  audit:
    enabled: ${APP_AUDIT_ENABLED:false}